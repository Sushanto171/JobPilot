"use client";
import { Eye, EyeOff, Lock, Mail, Zap } from "lucide-react";
import { useState } from "react";
import { COLORS, FieldInput, PrimaryButton, usePublicNavigation } from "./shared";
export function LoginPage() { const [show, setShow] = useState(false); const goTo = usePublicNavigation(); return <div className="min-h-[80vh] flex items-center justify-center px-5 py-16 page-in"><div className="w-full max-w-sm p-6 rounded-lg" style={{ backgroundColor: COLORS.surface, border: `1px solid ${COLORS.border}` }}><div className="text-center mb-6"><div className="w-8 h-8 rounded flex items-center justify-center mx-auto mb-3" style={{ backgroundColor: COLORS.amber }}><Zap size={16} color={COLORS.base} /></div><h1 className="text-base font-semibold" style={{ color: COLORS.paper, fontFamily: "monospace" }}>Welcome back</h1><p className="text-xs mt-1" style={{ color: COLORS.textDim }}>Sign in to your JobPilot account</p></div><div className="space-y-3.5"><FieldInput label="EMAIL" type="email" placeholder="you@example.com" icon={Mail} /><div><label className="text-xs" style={{ color: COLORS.textDim2, fontFamily: "monospace" }}>PASSWORD</label><div className="relative mt-1"><Lock size={14} color={COLORS.textDim} className="absolute left-3 top-1/2 -translate-y-1/2" /><input type={show ? "text" : "password"} placeholder="••••••••" className="w-full bg-transparent text-sm rounded-md pl-9 pr-9 py-2.5 outline-none" style={{ color: COLORS.paper, border: `1px solid ${COLORS.border}`, backgroundColor: COLORS.surfaceLight }} /><button onClick={() => setShow(!show)} className="absolute right-3 top-1/2 -translate-y-1/2">{show ? <EyeOff size={14} color={COLORS.textDim} /> : <Eye size={14} color={COLORS.textDim} />}</button></div></div><div className="flex justify-end"><button className="text-xs" style={{ color: COLORS.textDim }}>Forgot password?</button></div><PrimaryButton full>Sign in</PrimaryButton><div className="text-center text-xs" style={{ color: COLORS.textDim2 }}>Don't have an account? <button onClick={() => goTo("signup")} style={{ color: COLORS.cyan }}>Sign up</button></div></div></div></div>; }
