"use client";
import { ArrowRight } from "lucide-react";
import { COLORS, PrimaryButton, usePublicNavigation } from "./shared";
export function NotFoundPage() { const goTo = usePublicNavigation(); return <div className="min-h-[70vh] flex items-center justify-center px-5 page-in"><div className="text-center max-w-sm"><div className="flex justify-center mb-6"><div className="rounded-sm overflow-hidden flex shadow-2xl" style={{ width: "220px", backgroundColor: COLORS.paper }}><div style={{ width: "5px", backgroundColor: COLORS.rose }} /><div className="px-3 py-2.5 text-left"><div style={{ fontFamily: "monospace", fontSize: "10px", color: "#6B6558" }}>404 · UNKNOWN</div><div style={{ fontFamily: "monospace", fontWeight: 700, fontSize: "13px", color: "#1C1A15" }}>PAGE NOT FOUND</div><div style={{ fontSize: "11px", color: "#4A4638" }}>stage: LOST</div></div></div></div><h1 style={{ fontFamily: "monospace", fontWeight: 700, fontSize: "22px", color: COLORS.paper }}>This flight strip went missing.</h1><p className="text-sm mt-2" style={{ color: COLORS.textDim }}>The page you're looking for doesn't exist, or it moved racks.</p><div className="mt-6 flex justify-center"><PrimaryButton onClick={() => goTo("home")}>Back to home <ArrowRight size={14} /></PrimaryButton></div></div></div>; }
