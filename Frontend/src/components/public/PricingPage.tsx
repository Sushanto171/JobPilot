"use client";
import { Check } from "lucide-react";
import { COLORS, GhostButton, PrimaryButton, Reveal, SectionTag, usePublicNavigation } from "./shared";
const PLANS = [{ name: "Free", price: "$0", period: "forever", highlight: false, features: ["20 applications / month", "Manual resume tailoring review", "Basic pipeline board", "7-day follow-up reminders", "Community support"] }, { name: "Pro", price: "$9", period: "/ month", highlight: true, features: ["Unlimited applications", "AI resume tailoring + audit trail", "Full analytics dashboard", "Custom follow-up scheduling", "Email integration", "Priority support"] }, { name: "Teams", price: "Custom", period: "", highlight: false, features: ["Everything in Pro", "Multi-seat workspace", "Shared resume templates", "Admin usage dashboard", "SSO on request"] }];
export function PricingPage() { const goTo = usePublicNavigation(); return <div className="max-w-6xl mx-auto px-5 py-16 page-in"><Reveal className="text-center"><SectionTag>PRICING</SectionTag><h1 className="mt-2" style={{ fontFamily: "monospace", fontWeight: 700, fontSize: "38px", color: COLORS.paper }}>Simple pricing. No surprise seats.</h1><p className="mt-3 text-sm" style={{ color: COLORS.textDim }}>Start free. Upgrade only when you're applying at real volume.</p></Reveal><div className="grid grid-cols-1 md:grid-cols-3 gap-5 mt-12">{PLANS.map((plan, i) => <Reveal key={plan.name} delay={i * 100}><div className="h-full p-6 rounded-lg relative" style={{ backgroundColor: plan.highlight ? COLORS.surfaceLight : COLORS.surface, border: `1px solid ${plan.highlight ? COLORS.amber : COLORS.border}` }}>{plan.highlight && <span className="absolute -top-3 left-6 text-xs px-2.5 py-1 rounded-full font-semibold" style={{ backgroundColor: COLORS.amber, color: COLORS.base }}>Most popular</span>}<div className="text-sm font-semibold" style={{ color: COLORS.paper, fontFamily: "monospace" }}>{plan.name}</div><div className="flex items-baseline gap-1 mt-2"><span style={{ fontFamily: "monospace", fontWeight: 700, fontSize: "32px", color: COLORS.paper }}>{plan.price}</span><span className="text-xs" style={{ color: COLORS.textDim2 }}>{plan.period}</span></div><div className="mt-5 space-y-2.5">{plan.features.map((feature) => <div key={feature} className="flex items-start gap-2"><Check size={13} color={COLORS.cyan} /><span className="text-xs" style={{ color: COLORS.textDim }}>{feature}</span></div>)}</div><div className="mt-6">{plan.highlight ? <PrimaryButton full onClick={() => goTo("signup")}>Start Pro trial</PrimaryButton> : <GhostButton full onClick={() => goTo(plan.name === "Teams" ? "contact" : "signup")}>{plan.name === "Teams" ? "Contact sales" : "Get started"}</GhostButton>}</div></div></Reveal>)}</div></div>; }
