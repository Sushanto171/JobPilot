"use client";
import { Lock, Mail, User, Zap } from "lucide-react";
import { COLORS, FieldInput, PrimaryButton, usePublicNavigation } from "./shared";
export function SignupPage() { const goTo = usePublicNavigation(); return <div className="min-h-[80vh] flex items-center justify-center px-5 py-16 page-in"><div className="w-full max-w-sm p-6 rounded-lg" style={{ backgroundColor: COLORS.surface, border: `1px solid ${COLORS.border}` }}><div className="text-center mb-6"><div className="w-8 h-8 rounded flex items-center justify-center mx-auto mb-3" style={{ backgroundColor: COLORS.amber }}><Zap size={16} color={COLORS.base} /></div><h1 className="text-base font-semibold" style={{ color: COLORS.paper, fontFamily: "monospace" }}>Create your account</h1><p className="text-xs mt-1" style={{ color: COLORS.textDim }}>Free for up to 20 applications a month</p></div><div className="space-y-3.5"><FieldInput label="FULL NAME" placeholder="Your name" icon={User} /><FieldInput label="EMAIL" type="email" placeholder="you@example.com" icon={Mail} /><FieldInput label="PASSWORD" type="password" placeholder="At least 8 characters" icon={Lock} /><PrimaryButton full>Create account</PrimaryButton><div className="text-center text-xs" style={{ color: COLORS.textDim2 }}>Already have an account? <button onClick={() => goTo("login")} style={{ color: COLORS.cyan }}>Sign in</button></div></div></div></div>; }
